import React from 'react';

// Libraries
import styled from 'styled-components';

// Components
import {H3, P3} from '@components';

const Stage = styled.div`
  width: 100%;
  max-width: var(--nav-stage-width);
  height: 100%;
  min-height: 350px;
`;

export default Stage;
